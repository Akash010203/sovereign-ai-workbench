# rag/ (reserved)

Empty until **Phase 12** (see `docs/ARCHITECTURE.md` §11).

Planned contents: `ingest.py`, `chunking.py`, `embeddings.py` (local
only — no cloud embedding API), `index.py` (local vector index),
`retriever.py`, `citations.py`. Built only after the model
abstraction, router, tools, and agent foundations exist, per the
build contract.
