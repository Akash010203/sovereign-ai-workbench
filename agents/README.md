# agents/ (reserved)

Empty until **Phase 11** (see `docs/ARCHITECTURE.md` §11).

Planned contents: `planner.py`, `executor.py`, `verifier.py`,
`memory.py`, `state.py` — implementing the
REQUEST → PLAN → SELECT MODEL → SELECT TOOLS → EXECUTE → OBSERVE →
VERIFY → ITERATE → DELIVERABLE loop described in
`docs/ARCHITECTURE.md` §5.
