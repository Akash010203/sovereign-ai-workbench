# security/ (reserved)

Empty until **Phase 18** (see `docs/ARCHITECTURE.md` §11).

Planned contents: `permissions.py` (filesystem allowlist, tool
permissions), `audit.py`, `offline_mode.py`, `network_monitor.py`
(produces the *visible evidence* of zero outbound network calls
referenced in `docs/ARCHITECTURE.md` §6 and demo E).
