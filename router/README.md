# router/ (reserved)

Empty until **Phase 9** (see `docs/ARCHITECTURE.md` §11).

Planned contents: `router.py` (routes a request to a model/tool
pipeline), `task_classifier.py` (rule-based task classification —
labelled honestly as rule-based, not "AI intelligence", unless a
learned classifier is separately built and documented), `policies.py`.
